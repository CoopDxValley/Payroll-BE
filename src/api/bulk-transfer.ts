import { BulkTransferResponse } from "../modules/Payroll/payroll.type";

export async function sendBulkTransfer(
  id: string
): Promise<BulkTransferResponse> {
  return {} as BulkTransferResponse;
}
